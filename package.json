{
	"name": "obsidian-rich-table",
	"version": "1.4.0",
	"description": "Spreadsheet-like tables in a code block: merged cells, typed columns, sort, filter and summary rows, frozen panes, multi-sheet workbooks, and Kanban or calendar views of the same rows — with full Markdown and wikilinks in every cell.",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "node esbuild.config.mjs generate && tsc -noEmit -skipLibCheck && eslint . && node esbuild.config.mjs production",
		"test": "vitest run",
		"test:e2e": "playwright test",
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"lint": "eslint .",
		"lockfile:normalize": "node lockfile-normalize.mjs",
		"verify:ci": "node -e \"require('fs').rmSync('node_modules',{recursive:true,force:true})\" && npm ci && npm run build && npm test"
	},
	"keywords": [],
	"license": "AGPL-3.0",
	"devDependencies": {
		"@eslint/js": "^9.39.4",
		"@playwright/test": "^1.62.1",
		"@types/node": "^22.15.17",
		"esbuild": "0.28.2",
		"eslint": "^9.39.4",
		"eslint-plugin-obsidianmd": "^0.4.1",
		"globals": "^17.6.0",
		"jiti": "^2.6.1",
		"obsidian": "latest",
		"typescript": "^5.8.3",
		"typescript-eslint": "^8.59.1",
		"vitest": "^4.1.10"
	},
	"dependencies": {
		"@office-kit/xlsx": "^0.11.0",
		"html-to-image": "^1.11.13"
	}
}
